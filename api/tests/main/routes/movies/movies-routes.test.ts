import request from 'supertest'
import app from '@/main/config/app'

describe('Movies Routes', () => {
  it('Should return 200 on list movies success', async () => {
    /* await request(app)
      .get('/movies?page=1')
      .send({
        page: 1
      })
      .expect(200) */
  })
})
