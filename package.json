{
    "name": "movie-tmdb",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "api:build": "npm build --prefix api/",
        "api:start": "npm start --prefix api/",
        "site:build": "npm build --prefix site/",
        "site:start": "npm start --prefix site/"
    }
}